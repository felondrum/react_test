import React, { FC } from "react";
import IPage from "./IPage.tsx"

const HomePage: FC<IPage> = ({}: IPage) => {
    return (
        <>
        <div>Чистая страница</div>
        </>
    )
}

export default HomePage